// exercise 12
var members = ['sara', 'yousuf', 'zain', 'amna', 'ayesha'];
var message = 'all classes are suspended :';
for (var i = 0; i < members.length; i++) {
    console.log(message + members[i]);
}
